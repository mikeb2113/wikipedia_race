NOTE: In order to run this project, you will need to have docker and maven installed. You can verify that you have maven by running "mvn -version" in the terminal.
You can verify that you have docker by running "docker info" in the terminal

(Commands for windows machines)